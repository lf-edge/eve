image: eve-wlan
org: lfedge
network: yes
config:
  binds:
    - /lib/modules:/lib/modules
    - /lib/firmware:/lib/firmware
    - /dev:/dev
    - /run:/run
    - /var/config:/config
  net: host
  capabilities:
    - all
