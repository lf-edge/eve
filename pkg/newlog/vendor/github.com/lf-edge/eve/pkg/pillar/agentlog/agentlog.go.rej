diff a/pkg/pillar/agentlog/agentlog.go b/pkg/pillar/agentlog/agentlog.go	(rejected hunks)
@@ -123,6 +123,16 @@ func handleSignals(sigs chan os.Signal) {
 	}
 }
 
+// PrintStacks - for newlogd
+func PrintStacks() {
+	stacks := getStacks(false)
+	stackArray := strings.Split(stacks, "\n\n")
+	log.Errorf("Fatal stack trace due to %s with %d stack traces", savedRebootReason, len(stackArray))
+	for _, stack := range stackArray {
+		log.Errorf("%v", stack)
+	}
+}
+
 // Print out our stack
 func printStack() {
 	stacks := getStacks(false)
