image: eve-lisp
org: lfedge
network: yes
config:
  binds:
    - /dev:/dev
    - /etc/resolv.conf:/etc/resolv.conf
    - /run:/run
    - /var/persist:/persist
  net: host
  capabilities:
    - all
  pid: host
