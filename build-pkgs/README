This directory contains docker containers which setup build
environments for specific packages.

For eg: if you want to build an aports package, you do something
like so:

FROM zededa/build-pkgs_abuild AS abuild-tools
...

etcetera